[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--include=optional"

# SPA fallback - serve index.html for client-side routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
